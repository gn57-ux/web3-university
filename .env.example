# Privy Dashboard（https://dashboard.privy.io/）创建应用后获取，替换为你自己的值。
# 真实值只写入本地 .env.local（已被 .gitignore 排除），不要提交到仓库。
NEXT_PUBLIC_PRIVY_APP_ID=your-privy-app-id

# 完课确认受信任提交者私钥（服务端专用，无 NEXT_PUBLIC_ 前缀，不会打包进浏览器）。
# 本地演示固定使用 Anvil 默认测试账户 #2 的私钥（`anvil` 启动时终端会打印这批账户，
# 公开已知、零真实价值，仅用于本地链）；对应地址已在
# contracts/web3-university/script/DeployAllLocal.s.sol 里注册为
# DemoCompletionOracle 的受信任提交者。真实值只写入本地 .env.local，这里只放
# 占位符，绝不能填任何真实网络的私钥。
TRUSTED_SUBMITTER_PRIVATE_KEY=your-local-anvil-submitter-private-key
